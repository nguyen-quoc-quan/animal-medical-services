%h1 Listing food_specifications
%table
  %thead
    %tr
      %th Capacity
      %th Food specification types
      %th{:colspan => "3"}
  %tbody
    - @food_specifications.each do |food_specification|
      %tr
        %td= food_specification.capacity
        %td= food_specification.food_specification_types_id
        %td= link_to 'Show', food_specification
        %td= link_to 'Edit', edit_food_specification_path(food_specification)
        %td= link_to 'Destroy', food_specification, method: :delete, data: { confirm: 'Are you sure?' }
%br/
= link_to 'New Food specification', new_food_specification_path
